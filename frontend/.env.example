# Variáveis de ambiente para o frontend do Pomodash

# URL base da API do backend
# Em ambiente de desenvolvimento, geralmente é http://localhost:3001/api
# Em produção, deve ser a URL do seu backend no Render
VITE_API_BASE_URL=http://localhost:3001/api